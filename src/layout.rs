pub enum LayoutElement {
    Prefix,
    Current,
    Total,
    Percent,
    Bar,
    TimeLeft,
    TimeElapsed,
    Speed,
    Message,
}

pub enum LayoutAlignment {
    Left,
    Center,
    Right,
}